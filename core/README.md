# Language-specific Frameworks for OpenFABR CDF

The directory names should follow **(language)** convention, such as `typescript`. 
They are IaC runtime-independent, therefore no need to include **runtime** information.
